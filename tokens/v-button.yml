#  ==========================================================================
# buttons.yml
#
#   TOC:
#
#  Base
#  Secondary
#  Cta-sale
#  Size Variants
#    sm
#    xs
#  ==========================================================================
props:
  button-background-color:
    value: "{!easily-excited}"
    type: color
  button-color:
    value: "{!clean-slate}"
    type: color
  button-border-radius:
    value: "{!radius-softer}"
  button-box-shadow: 
    value: 'inset 0 0 0 1px {!closed-on-monday}'
  # States
  ## Hover & Active
  button-hover-background-color:
    value: "{!closed-on-monday}"
    type: color
  button-hover-box-shadow:
    value: 'inset 0 0 0 1px {!closed-on-monday}'
  button-hover-color:
    value: "{!clean-slate}"
    type: color
  ## disabled
  button-disabled-background-color: 
    value: "{!partly-cloudy}"
    type: color
  button-disabled-box-shadow: 
    value: 'inset 0 0 0 1px {!grey-matter}'
  button-disabled-color: 
    value: "{!clean-slate}"
    type: color
#
# Modifier style variants
#  ==========================================================================
# Secondary
#  ==========================================================================
  button-secondary-color:
    value: "{!heart-of-darkness}"
    type: color
  button-secondary-background-color:
    value: "{!clean-slate}"
    type: color
  button-secondary-box-shadow:
    value: 'inset 0 0 0 1px {!taken-for-granite}'

  # States
  ## Hover & Active
  button-secondary-hover-color:
    value: "{!heart-of-darkness}"
    type: color
  button-secondary-hover-background-color:
    value: "{!the-tooth}"
    type: color
  ## disabled
  button-secondary-disabled-background-color:
    value: "{!clean-slate}"
    type: color
  button-secondary-disabled-color:
    value: "{!grey-matter}"
    type: color
  button-secondary-disabled-box-shadow:
    value: 'inset 0 0 0 1px {!partly-cloudy}'
#
# Modifier style variants
#  ==========================================================================
# CTA-Brand
#  ==========================================================================
  button-cta-brand-color:
    value: "{!clean-slate}"
    type: color
  button-cta-brand-background-color:
    value: "{!easily-excited}"
    type: color
  button-cta-brand-box-shadow:
    value: 'inset 0 0 0 1px {!closed-on-monday}'
  # States
  ## Hover & Active
  button-cta-brand-hover-color:
    value: "{!clean-slate}"
    type: color
  button-cta-brand-hover-background-color:
    value: '{!closed-on-monday}'
    type: color

#  ==========================================================================
# CTA-Dark
#  ==========================================================================
  button-cta-dark-color:
    value: "{!clean-slate}"
    type: color
  button-cta-dark-background-color:
    value: "{!heart-of-darkness}"
    type: color
  button-cta-dark-box-shadow:
    value: 'inset 0 0 0 1px {!threat-level-midnight}'
  # States
  ## Hover & Active
  button-cta-dark-hover-color:
    value: "{!clean-slate}"
    type: color
  button-cta-dark-hover-background-color:
    value: '{!threat-level-midnight}'
    type: color


#  ==========================================================================
# CTA-Light
#  ==========================================================================
  button-cta-light-color:
    value: "{!heart-of-darkness}"
    type: color
  button-cta-light-background-color:
    value: "{!clean-slate}"
    type: color
  button-cta-light-box-shadow:
    value: 'inset 0 0 0 1px {!taken-for-granite}'
  # States
  ## Hover & Active
  button-cta-light-hover-color:
    value: "{!heart-of-darkness}"
    type: color
  button-cta-light-hover-background-color:
    value: '{!the-tooth}'
    type: color

#  ==========================================================================
# CTA-Sale
#  ==========================================================================
  button-cta-sale-color:
    value: "{!clean-slate}"
    type: color
  button-cta-sale-background-color:
    value: "{!high-stakes}"
    type: color
  button-cta-sale-box-shadow:
    value: 'inset 0 0 0 1px {!quick-fixe}'
  # States
  ## Hover & Active
  button-cta-sale-hover-color:
    value: "{!clean-slate}"
    type: color
  button-cta-sale-hover-background-color:
    value: '{!quick-fixe}'
    type: color

global:
  category: button
  type: token
imports:
  - ./foundations/_index.yml